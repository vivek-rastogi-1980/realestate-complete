const lenis = new Lenis({lerp:.08,smoothWheel:true});function raf(t){lenis.raf(t);requestAnimationFrame(raf)}requestAnimationFrame(raf);gsap.registerPlugin(ScrollTrigger);gsap.utils.toArray('.reveal').forEach(el=>gsap.from(el,{opacity:0,y:70,scale:.96,duration:1.1,ease:'power3.out',scrollTrigger:{trigger:el,start:'top 82%'}}));document.addEventListener('pointermove',e=>{gsap.to('.cursor-dot',{x:e.clientX-9,y:e.clientY-9,duration:.18});document.querySelectorAll('.property-card').forEach(card=>{const r=card.getBoundingClientRect(),x=e.clientX-r.left-r.width/2,y=e.clientY-r.top-r.height/2;card.style.transform=`rotateY(${x/60}deg) rotateX(${-y/60}deg)`})});new Swiper('.testimonial-swiper',{loop:true,autoplay:{delay:2600},effect:'creative',creativeEffect:{prev:{translate:['-120%',0,-500]},next:{translate:['120%',0,-500]}}});function chart(id){const el=document.getElementById(id);if(el)new Chart(el,{type:'line',data:{labels:['Q1','Q2','Q3','Q4'],datasets:[{data:[12,19,28,41],borderColor:'#d8b66a',backgroundColor:'#54a8ff33',fill:true,tension:.45}]},options:{plugins:{legend:{display:false}},scales:{x:{display:false},y:{display:false}}}})}chart('revenueChart');chart('adminChart');const canvas=document.getElementById('buildingScene');if(canvas){const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(55,innerWidth/innerHeight,.1,100),renderer=new THREE.WebGLRenderer({canvas,alpha:true,antialias:true});renderer.setSize(innerWidth,innerHeight);camera.position.z=8;const group=new THREE.Group();for(let i=0;i<22;i++){const geo=new THREE.BoxGeometry(.5+Math.random(),1+Math.random()*4,.5+Math.random());const mat=new THREE.MeshStandardMaterial({color:i%3?0x1a2433:0xd8b66a,metalness:.7,roughness:.18,transparent:true,opacity:.86});const m=new THREE.Mesh(geo,mat);m.position.set((Math.random()-.5)*8,Math.random()*2-1,(Math.random()-.5)*4);group.add(m)}scene.add(group);scene.add(new THREE.PointLight(0x54a8ff,80,20));scene.add(new THREE.AmbientLight(0xffffff,.9));function tick(){group.rotation.y+=.003;renderer.render(scene,camera);requestAnimationFrame(tick)}tick();addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)})}
